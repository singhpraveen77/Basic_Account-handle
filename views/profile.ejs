<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Document</title>
    <script src="https://cdn.tailwindcss.com"></script>

</head>
<body>
    <div class="bg-zinc-800 min-h-screen flex flex-col p-10">
        <h1 class="text-4xl font-bold text-yellow-400 mb-6"> hey , <%= user.username %> ✌️</h1>
        <p class="text-white mb-4">Want to create a new post?</p>
        <form action="/posts" method="post" class="mb-6">
            <textarea name="content" class="w-full h-32 p-2 bg-gray-800 text-white rounded-lg focus:outline-none focus:ring-2 focus:ring-blue-500 mb-4" placeholder="Write your post here..."></textarea>
            <input type="submit" value="Post" class="bg-green-500 hover:bg-green-700 text-white font-bold py-2 px-4 rounded cursor-pointer">
        </form>
        <div class="mt-6">
            <h1 class="text-2xl font-semibold text-white mb-4">Your Posts</h1>
            <div class="space-y-4">
                <div class="flex flex-wrap">
                    <% user.posts.forEach((ele)=>{ %>
                        <div class="bg-gray-700 p-4 rounded-lg shadow-md w-1/3 m-2">
                            <a href="#" class="text-yellow-400 font-bold">@ <%= user.username %></a>
                            <p class="text-white mt-2"><%= ele.content %></p>
                            <div class="mt-2 flex space-x-4">
                                <a href="#" class="text-blue-400 hover:text-blue-600">Like</a>
                                <a href="#" class="text-green-400 hover:text-green-600">Edit</a>
                            </div>
                        </div>
                    <% }) %>
                </div>
            </div>
            </div>
        </div>
        </div>
        <a href="/logout" class="fixed top-4 right-4 bg-red-500 hover:bg-green-700 text-white font-bold py-2 px-4 rounded">
            Logout
        </a>




        
    </div>
    
</body>
</html>