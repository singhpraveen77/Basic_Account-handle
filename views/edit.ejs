<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Update Post</title>
    <script src="https://cdn.tailwindcss.com"></script>
</head>
<body>
    <div class="bg-zinc-900 min-h-screen flex flex-col  p-6">
        <h1 class="text-3xl font-bold text-yellow-400 mb-4">Update Your Post</h1>
        <p class="text-gray-300 mb-6">Modify your post content below:</p>
        <form action="/update/<%= post._id %>" method="post" class="w-full max-w-lg">
            <textarea name="content" class="w-full h-40 p-3 bg-gray-800 text-white rounded-lg focus:outline-none focus:ring-2 focus:ring-yellow-500 mb-4" placeholder="Edit your post here..."></textarea>
            <input type="submit" value="Update" class="bg-yellow-500 hover:bg-yellow-800 text-white font-bold py-2 px-6 rounded cursor-pointer transition duration-300 ease-in-out transform hover:scale-105">
        </form>
    </div>
</body>
</html>